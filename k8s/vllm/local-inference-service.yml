apiVersion: v1
kind: Service
metadata:
  name: local-inference
  namespace: llm-inference
  labels:
    app: local-inference
spec:
  type: ClusterIP
  selector:
    app: local-inference
  ports:
    - name: http
      port: 8000
      targetPort: 8000